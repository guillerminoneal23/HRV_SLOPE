# Cleanup Audit Report — HRV Slope App

**Date:** 2026-05-27  
**Scope:** Phase 4.0C Step 1 — Project Cleanup Audit Only  
**Status:** Audit complete; no files moved or deleted

---

## 1. Executive Summary

The repository is functionally healthy. The Flutter app remains at schema version 4, analyzer is clean, and all tests pass. No behavioral, schema, fixture, or generated-code changes were made during this audit.

The main cleanup opportunity is organizational rather than technical:

- The parent project root contains many phase reports that should move to `docs/phase_reports/`.
- Research/source extraction artifacts should move to `docs/archive/` or a future `docs/references/` folder.
- Local/generated Flutter build artifacts exist in the app directory and should stay ignored; they are safe cleanup targets only if regenerated by Flutter/Dart tooling.
- The app `README.md` is still the default Flutter starter README and should be rewritten in a later docs pass.
- Several old phase reports contain historically accurate but now stale statements. These should be archived as phase snapshots rather than edited in place unless the team wants a living changelog.

Recommended Step 2 cleanup should be limited to moving documentation/artifacts and optionally deleting ignored local build caches. Do not remove Drift generated files, test fixtures, platform files, or source modules.

---

## 2. Current Verified State

Verified from `hrv_slope_app/`:

| Check | Result |
|---|---|
| Test command | `C:\flutter\bin\cache\dart-sdk\bin\dart.exe C:\flutter\bin\cache\flutter_tools.snapshot test` |
| Test result | `302/302 passing` |
| Analyze command | `C:\flutter\bin\cache\dart-sdk\bin\dart.exe C:\flutter\bin\cache\flutter_tools.snapshot analyze` |
| Analyze result | `No issues found` |
| Schema version | `4` in `hrv_slope_app/lib/data/database/app_database.dart` |
| build_runner | Not run; no schema/generated changes |

Inventory highlights:

- Parent root markdown files: 14
- Flutter app source files under `lib/`: 49
- Test/fixture files under `test/`: 14
- Drift generated files: 4
- Real RR fixture files: 3 plus README
- `docs/` directory: not currently present

---

## 3. Files To Keep In Root

Keep these high-level living docs in the parent project root:

- `PROJECT_SPEC.md`
- `IMPLEMENTATION_PLAN.md`
- `DATA_MODEL.md`
- `ALGORITHM_SPEC.md`
- `HRV_RR_PREPROCESSING_SPEC.md`

Keep these app/project files in `hrv_slope_app/`:

- `README.md` — keep in place but rewrite later; it is currently the default Flutter starter README.
- `pubspec.yaml`
- `pubspec.lock`
- `analysis_options.yaml`
- `.metadata`
- platform folders: `ios/`, `windows/`
- source folders: `lib/`, `test/`

Keep generated/required files in place:

- `hrv_slope_app/lib/data/database/app_database.g.dart`
- `hrv_slope_app/lib/data/database/daos/athletes_dao.g.dart`
- `hrv_slope_app/lib/data/database/daos/sessions_dao.g.dart`
- `hrv_slope_app/lib/data/database/daos/settings_dao.g.dart`
- Flutter platform generated plugin registrants under `ios/Runner/` and `windows/flutter/`

---

## 4. Files Recommended To Move To `docs/`

No root files strictly require a move to plain `docs/` right now because the living specs are acceptable in root.

Optional future organization:

- Create `docs/references/` for papers and source workbooks if you prefer not to use `docs/archive/`.
- Create `docs/manual/` later for Phase 5 user-facing instructions.

---

## 5. Files Recommended To Move To `docs/phase_reports/`

Do not delete these reports. Move them as historical phase records:

- `PHASE_1_5_AUDIT_REPORT.md`
- `PHASE_2_COMPLETION_REPORT.md`
- `PHASE_2_1_COMPLETION_REPORT.md`
- `PHASE_2_2_COMPLETION_REPORT.md`
- `PHASE_2_3_COMPLETION_REPORT.md`
- `PHASE_3_0_COMPLETION_REPORT.md`
- `PHASE_3_1_COMPLETION_REPORT.md`
- `PHASE_4_0A_COMPLETION_REPORT.md`
- `PHASE_4_0B_COMPLETION_REPORT.md`

Count: 9 files.

---

## 6. Files Recommended To Move To `docs/archive/`

These appear to be source/reference artifacts or extraction scratch files. They are useful for traceability but should not crowd the project root before packaging:

- `2019_Orellana_Utility of the RMSSD-Slope to Assess the.pdf`
- `2019_Orellana.Recovery Slope of Heart Rate Variability as an __Indicator of Internal Training Load.pdf`
- `VALORACIÓN CARGA INTERNA.xlsx`
- `Recuperación HRV_Grupal.xlsm`
- `paper1_extract.txt`
- `paper2_extract.txt`
- `xlsm_extract.txt`

Count: 7 files.

Recommendation: if the PDFs/workbooks are canonical references, consider `docs/references/` instead of `docs/archive/`; keep the extracted `.txt` files in `docs/archive/` unless they are still actively used.

---

## 7. Candidate Deletion List With Risk Level

No source file is recommended for immediate deletion. The list below is limited to local/generated clutter or stale support files that should be handled cautiously.

| Path | Risk | Evidence | Recommendation |
|---|---|---|---|
| `hrv_slope_app/build/` | Low | Flutter build/test output; ignored by app `.gitignore` via `/build/` | Safe to delete locally when no process is running; Flutter recreates it |
| `hrv_slope_app/.dart_tool/` | Low | Dart/Flutter package/build cache; ignored by `.gitignore` | Safe to delete locally if dependency/build cache reset is desired |
| `hrv_slope_app/ios/Flutter/ephemeral/` | Low | Flutter-generated iOS ephemeral files | Safe to delete locally; Flutter recreates |
| `hrv_slope_app/windows/flutter/ephemeral/` | Low | Flutter-generated Windows ephemeral files | Safe to delete locally; Flutter recreates |
| `hrv_slope_app/.flutter-plugins-dependencies` | Low | Generated Flutter plugin metadata; ignored | Usually safe to regenerate, but no need to delete now |
| `hrv_slope_app/hrv_slope_app.iml` | Low/Medium | IntelliJ module file; ignored by `*.iml` | Delete only if not intentionally shared for IDE setup |
| `hrv_slope_app/.idea/` | Medium | IDE configuration; ignored by `.gitignore` | Delete only if local IDE state is unwanted |
| `.vscode/settings.json` | Medium | Parent-root local editor settings, file contains only `{}` | Candidate if not intentionally shared |

Do not delete:

- Drift generated `.g.dart` files
- RR fixtures
- platform source files
- `pubspec.*`
- `analysis_options.yaml`
- database table/DAO files
- any `lib/shared/engine/*` module

Code-module audit notes:

- No clearly unused `lib/` module was found by import/reference search.
- `csv_importer.dart` and `rmssd_csv_importer.dart` are both referenced and serve different workflows.
- `rr_quality.dart` remains used by RMSSD validation, calculation preview, RR widget, and tests.
- `session_constants.dart` and `hrv_sources.dart` remain used by wizard/edit/session services.
- `phase2_1_test.dart` still contains synthetic RR sample tests. These are somewhat redundant after mandatory real fixtures but still active regression coverage; do not delete without a deliberate test consolidation pass.

---

## 8. Stale Documentation Findings

These findings are mostly in historical phase reports. Since phase reports are snapshots, the safest cleanup is to move them under `docs/phase_reports/` and optionally add a short index saying older reports may describe work that has since been completed.

| File | Line/context | Finding | Suggested treatment |
|---|---:|---|---|
| `hrv_slope_app/README.md` | whole file | Default Flutter starter README, not HRV Slope specific | Rewrite in a later docs pass |
| `PHASE_1_5_AUDIT_REPORT.md` | 64, 82 | Says `68/68 tests passing`; later reports note runner consistently showed 67 engine tests and current total is 302 | Historical discrepancy; archive or annotate in phase report index |
| `PHASE_2_1_COMPLETION_REPORT.md` | 94-99, 158-159, 184 | Says RR samples were simulated and real RR files not found | Superseded by Phase 2.2b mandatory real fixture validation |
| `PHASE_2_COMPLETION_REPORT.md` | 145-148, 174-175 | Says RR paste UI, individual nomogram UI, and longitudinal charts were not started/deferred | Superseded by later phases |
| `PHASE_2_3_COMPLETION_REPORT.md` | 7 | Says no Phase 3 reports, longitudinal charts, individual nomogram UI, or export/PDF work was started | Historically true for Phase 2.3; now superseded for reports/longitudinal/nomogram |
| `PHASE_3_0_COMPLETION_REPORT.md` | 148-159 | Lists longitudinal trend and individual nomogram fitting UI as future work | Superseded by Phase 4.0A/4.0B |
| `PHASE_3_1_COMPLETION_REPORT.md` | 150-151, 185-186 | Says individual nomogram fitting UI and longitudinal dashboard remain deferred/next | Superseded by Phase 4.0A/4.0B |
| `PHASE_4_0A_COMPLETION_REPORT.md` | 149-150, 184 | Says individual nomogram fitting UI and hybrid overlay are deferred/next | Superseded by Phase 4.0B |
| `PROJECT_SPEC.md` | 237, 257, 275, 296, 315, 334 | Earlier phase sections contain "deferred/not started" statements for things now completed later | Acceptable if treated as chronological history; consider a "Historical phase notes" label |
| `IMPLEMENTATION_PLAN.md` | 303 | "3 simulated samples" in Phase 2.1 section remains in historical notes | Superseded by Phase 2.2b; acceptable as history but stale if read as current |

No stale current claims were found in:

- `PHASE_4_0B_COMPLETION_REPORT.md`
- `HRV_RR_PREPROCESSING_SPEC.md`

---

## 9. Test Fixture Audit

Verified fixture directory:

- `hrv_slope_app/test/fixtures/rr_samples/`

Required files present:

- `2026-05-25_05-27-02.txt`
- `2026-05-22_05-39-13.txt`
- `2026-05-21_05-42-46.txt`
- `README.md`

README status:

- States fixtures are anonymized RR interval files.
- States values are in milliseconds.
- States one RR interval per line.
- States they validate parsing, RR quality, preprocessing, and RMSSD calculation.
- States they are not used for medical diagnosis.

Mandatory test status:

- `test/phase2_2_test.dart` has `Mandatory real RR fixtures` tests that fail if the fixture directory or required files are missing.
- The helper `_readFixtureText` asserts each fixture file exists before reading.
- No conditional skip remains for these real fixture tests.
- Regression guards in later phase tests also assert required fixture files exist.

Do not delete or move `test/fixtures/rr_samples/`.

---

## 10. `.gitignore` And Build Artifact Recommendations

Current app `.gitignore` already covers:

- `*.log`
- `.idea/`
- `*.iml`
- `.dart_tool/`
- `.flutter-plugins-dependencies`
- `/build/`
- `/coverage/`
- common OS/editor artifacts

Recommendations:

- Add a parent-root `.gitignore`, or move existing project-level ignore coverage up one level if the repository root is `C:\Users\Guillermo\Downloads\HRV Slope_App`.
- Consider ignoring root `.vscode/` unless those settings are intentionally shared.
- Consider adding local app database patterns:
  - `*.sqlite`
  - `*.sqlite-shm`
  - `*.sqlite-wal`
  - `*.db`
- Consider adding temporary export patterns for future phases:
  - `exports/`
  - `tmp/`
  - `*.tmp`
  - `*.bak`
- Keep `pubspec.lock` tracked for the Flutter app.
- Keep generated Drift `.g.dart` files tracked unless the project explicitly changes generated-code policy.

No `.gitignore` edits were made in this audit step.

---

## 11. Proposed Safe Cleanup Plan For Step 2

Recommended sequence:

1. Create directories:
   - `docs/`
   - `docs/phase_reports/`
   - `docs/archive/`

2. Move phase reports:
   - Move the 9 `PHASE_*` markdown files into `docs/phase_reports/`.
   - Optionally create `docs/phase_reports/README.md` with phase order and note that reports are historical snapshots.

3. Move reference/archive artifacts:
   - Move the 2 PDFs, 2 workbooks, and 3 extracted text files into `docs/archive/` or split canonical references into `docs/references/`.

4. Leave living specs in root:
   - `PROJECT_SPEC.md`
   - `IMPLEMENTATION_PLAN.md`
   - `DATA_MODEL.md`
   - `ALGORITHM_SPEC.md`
   - `HRV_RR_PREPROCESSING_SPEC.md`

5. Rewrite app README:
   - Replace `hrv_slope_app/README.md` with an HRV Slope-specific project overview, setup commands, test commands, and local-first/privacy notes.

6. Optional local cleanup:
   - Delete `hrv_slope_app/build/` and `hrv_slope_app/.dart_tool/` only as local cache cleanup.
   - Do not delete generated Drift files.

7. Add or update ignore rules:
   - Add a parent-root `.gitignore` if this parent directory is the actual repo root.
   - Add local database/export temporary patterns before export features begin.

8. Verify:
   - Run `flutter analyze`.
   - Run `flutter test`.
   - Confirm fixture tests still pass.

Recommended move/archive/delete counts:

- Move to `docs/phase_reports/`: 9 files
- Move to `docs/archive/`: 7 files
- Move to plain `docs/`: 0 required now
- Candidate local/generated deletion targets: 8 paths

